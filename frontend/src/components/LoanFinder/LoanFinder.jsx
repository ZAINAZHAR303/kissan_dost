export const LoanFinder = () => {
    return (
      <section id="loans" className="p-8 bg-gray-100">
        <h2 className="text-3xl font-bold text-green-700 mb-6">Agriculture Loan & Subsidy Finder</h2>
        {/* <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {loans.map((loan, index) => (
            <div key={index} className="bg-white shadow-md rounded-lg p-4">
              <h3 className="text-xl font-semibold text-green-800">{loan.name}</h3>
              <p className="text-gray-600 mt-2">{loan.description}</p>
              <p className="text-green-600 mt-4 font-bold">Interest Rate: {loan.interestRate}</p>
              <p className="text-gray-600">Eligibility: {loan.eligibility}</p>
            </div>
          ))}
        </div> */}
      </section>
    );
  };
  